# Architecture

`packages/core` owns config, discovery, interpolation, compile, validate, build, and send helpers.

`packages/cli` owns terminal workflows.

`apps/studio` owns browser UI and API route integration.
